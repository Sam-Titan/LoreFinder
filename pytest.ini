[pytest]
testpaths = tests
markers =
    smoke: fast narrow-query checks, cheap to rerun
    full: broad-query checks that need full chapter summarization
    slow: large novels (100+ chapters) — expensive, run on demand
